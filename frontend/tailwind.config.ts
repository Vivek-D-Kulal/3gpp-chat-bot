import type { Config } from "tailwindcss";

export default {
	darkMode: ["class"],
	content: [
		"./pages/**/*.{ts,tsx}",
		"./components/**/*.{ts,tsx}",
		"./app/**/*.{ts,tsx}",
		"./src/**/*.{ts,tsx}",
	],
	prefix: "",
	theme: {
		container: {
			center: true,
			padding: '2rem',
			screens: {
				'2xl': '1400px'
			}
		},
		extend: {
			colors: {
				border: 'hsl(var(--border))',
				input: 'hsl(var(--input))',
				ring: 'hsl(var(--ring))',
				background: 'hsl(var(--background))',
				foreground: 'hsl(var(--foreground))',
				primary: {
					DEFAULT: 'hsl(var(--primary))',
					foreground: 'hsl(var(--primary-foreground))',
					hover: 'hsl(var(--primary-hover))'
				},
				secondary: {
					DEFAULT: 'hsl(var(--secondary))',
					foreground: 'hsl(var(--secondary-foreground))',
					hover: 'hsl(var(--secondary-hover))'
				},
				// Chat-specific colors
				'chat-background': 'hsl(var(--chat-background))',
				'chat-user-bg': 'hsl(var(--chat-user-bg))',
				'chat-assistant-bg': 'hsl(var(--chat-assistant-bg))',
				'chat-user-text': 'hsl(var(--chat-user-text))',
				'chat-assistant-text': 'hsl(var(--chat-assistant-text))',
				'chat-border': 'hsl(var(--chat-border))',
				'chat-input-bg': 'hsl(var(--chat-input-bg))',
				'chat-input-border': 'hsl(var(--chat-input-border))',
				// Graph-specific colors
				'graph-background': 'hsl(var(--graph-background))',
				'graph-border': 'hsl(var(--graph-border))',
				'graph-highlight': 'hsl(var(--graph-highlight))',
			},
			borderRadius: {
				lg: 'var(--radius)',
				md: 'calc(var(--radius) - 2px)',
				sm: 'calc(var(--radius) - 4px)'
			},
			keyframes: {
				'accordion-down': {
					from: {
						height: '0'
					},
					to: {
						height: 'var(--radix-accordion-content-height)'
					}
				},
				'accordion-up': {
					from: {
						height: 'var(--radix-accordion-content-height)'
					},
					to: {
						height: '0'
					}
				}
			},
			animation: {
				'accordion-down': 'accordion-down 0.2s ease-out',
				'accordion-up': 'accordion-up 0.2s ease-out'
			}
		}
	},
	plugins: [require("tailwindcss-animate")],
} satisfies Config;
